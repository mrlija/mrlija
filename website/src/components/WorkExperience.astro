---
import { CV_DATA } from "../data/cv";
import Item from "./common/Item.astro";

const { work } = CV_DATA;
---

<section class="section-block">
  <h2 class="section-heading" id="work-experience">Pracovní zkušenosti</h2>
  {
    work.map((item) => (
      <Item
        title={item.title}
        description={item.description}
        link={item.link}
        start={item.start}
        end={item.end}
        badges={item.badges}
      />
    ))
  }
</section>
